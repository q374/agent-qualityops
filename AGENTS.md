# Agent QualityOps 开发规则

- 开始或恢复任务时先读 `MEMORY.md`，再读与当前模块相关的代码和 `docs/API_CONTRACT.md`。
- 使用标准简体中文维护产品文档；代码标识符沿用英文。
- 示例数据只能来自公开资料或明确标注的合成输入，不添加企业内部数据、个人信息或不可核验指标。
- API 密钥只从本地环境变量或未跟踪的 `.env` 读取，不打印、不回传、不提交。
- `demo` 结果只能用于流程演示；没有真实模型运行与人工复核时，不写成模型质量结论。
- 修改后运行相关后端测试和前端构建；界面变更还需在浏览器走通主流程。
- 不通过跳过断言、吞错或修改期望值隐藏 Badcase。自动评分、人工复核和发布门禁结果必须分别保留。
- 生产部署、比赛提交、GitHub 发帖或 PR、真实模型付费调用需按当前授权单独核对。
